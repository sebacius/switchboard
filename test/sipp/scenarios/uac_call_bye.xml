<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  UAC: Make a call, wait 5s, then hang up.
  Pass caller via -s flag, callee via -key callee
  Example: sipp ... -s 2001 -key callee 1001
-->
<scenario name="UAC - Call and BYE">
  <!-- Send INVITE -->
  <send retrans="500">
    <![CDATA[
INVITE sip:[callee]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:[service]@[remote_ip]:[remote_port]>;tag=[pid]uac[call_number]
To: <sip:[callee]@[remote_ip]:[remote_port]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
Content-Type: application/sdp
User-Agent: SIPp/TotallyNotARobot/1.0
Content-Length: [len]

v=0
o=sipp 0 0 IN IP4 [local_ip]
s=-
c=IN IP4 [local_ip]
t=0 0
m=audio [media_port] RTP/AVP 0
a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <!-- Expect provisional responses -->
  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="183" optional="true"/>

  <!-- Receive 200 OK -->
  <recv response="200" rtd="true"/>

  <!-- Send ACK to the server (same destination as INVITE) -->
  <send>
    <![CDATA[
ACK sip:[callee]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:[service]@[remote_ip]:[remote_port]>;tag=[pid]uac[call_number]
[last_To:]
Call-ID: [call_id]
CSeq: 1 ACK
Max-Forwards: 70
Content-Length: 0
    ]]>
  </send>

  <!-- Call duration: 5 seconds -->
  <pause milliseconds="5000"/>

  <!-- Send BYE -->
  <send retrans="500">
    <![CDATA[
BYE sip:[callee]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:[service]@[remote_ip]:[remote_port]>;tag=[pid]uac[call_number]
[last_To:]
Call-ID: [call_id]
CSeq: 2 BYE
Max-Forwards: 70
Content-Length: 0
    ]]>
  </send>

  <!-- Receive 200 OK for BYE -->
  <recv response="200"/>
</scenario>
