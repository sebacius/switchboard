<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  UAC: simulate an inbound trunk call to a MAPPED DID and expect the gate to
  accept it (100 Trying + normal answer, then server-driven BYE after playback).
  Requires the sipp host IP to be configured as a trunk peer in trunk_peers.json
  and the DID present in routes.json.
  Pass an unregistered From via -s, the mapped DID via -key callee.
  Example: sipp ... -s 15550000000 -key callee +15551234567
-->
<scenario name="UAC - Trunk mapped DID (expect accept)">
  <!-- Send INVITE -->
  <send retrans="500">
    <![CDATA[
INVITE sip:[callee]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: <sip:[service]@[remote_ip]:[remote_port]>;tag=[pid]uac[call_number]
To: <sip:[callee]@[remote_ip]:[remote_port]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
Content-Type: application/sdp
User-Agent: SIPp/TotallyNotARobot/1.0
Content-Length: [len]

v=0
o=sipp 0 0 IN IP4 [local_ip]
s=-
c=IN IP4 [local_ip]
t=0 0
m=audio [media_port] RTP/AVP 0
a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <!-- Gate accepts: 100 Trying then normal answer. -->
  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="183" optional="true"/>
  <recv response="200" rtd="true"/>

  <!-- ACK the answer -->
  <send>
    <![CDATA[
ACK [next_url] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: [last_From:]
To: [last_To:]
Call-ID: [call_id]
CSeq: 1 ACK
Max-Forwards: 70
Content-Length: 0
    ]]>
  </send>

  <!-- Server sends BYE after audio playback -->
  <recv request="BYE"/>

  <send>
    <![CDATA[
SIP/2.0 200 OK
[last_Via:]
[last_From:]
[last_To:]
[last_Call-ID:]
[last_CSeq:]
Content-Length: 0
    ]]>
  </send>
</scenario>
