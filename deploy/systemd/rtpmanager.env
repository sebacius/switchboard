# Switchboard RTP Manager Configuration
# Copy to /etc/switchboard/rtpmanager.env

# gRPC server settings
GRPC_PORT=9090
GRPC_BIND=0.0.0.0

# Address advertised in SDP (auto-detected if empty)
RTP_ADVERTISE=

# RTP port range
RTP_PORT_MIN=10000
RTP_PORT_MAX=20000

# Audio files base path
AUDIO_PATH=/opt/switchboard/audio
