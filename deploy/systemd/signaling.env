# Switchboard SIP Signaling Configuration
# Copy to /etc/switchboard/signaling.env

# SIP server settings
SIP_PORT=5060
SIP_BIND=0.0.0.0

# Address advertised to SIP clients (required)
SIP_ADVERTISE=your-server-ip

# RTP Manager addresses (comma-separated for multiple)
RTPMANAGER_ADDRS=localhost:9090

# Dialplan configuration file
DIALPLAN_PATH=/opt/switchboard/dialplan.json

# Logging level: debug, info, warn, error
LOG_LEVEL=info
